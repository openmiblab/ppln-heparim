name: heparim
channels:
  - pytorch
  - nvidia  # Add this if you have an NVIDIA GPU
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip

  - pip:
      - -e .    # This installs the pipeline from source
      - dbdicom